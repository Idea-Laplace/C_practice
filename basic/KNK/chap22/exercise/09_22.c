// Which one of the following calls is not a valid way of rading one character
// from the standard input stream?

// (a) getch();
// (b) getchar();
// (c) getc(stdin);
// (d) fgetc(stdin);

// AnS) (a) getch(); it is not a standard function
