/*
   Indicate  whether each fo the following files is more likely to 
   contain text data or binary data:

   (a) A file of object code produced by a C complier.
   Ans) Binary.
   (b) A program listing produced by a C compiler.
   Ans) Text.
   (c) An email message sent from one computer to another
   Ans) Text.
   (d) A file containing a graphics image.
   Ans) Binary.


   Text files have 2 characteristic that binary files don't possess:

   1) Text files are divided into lines.
   2) Text files may contain a special end-of-file marker.
*/
