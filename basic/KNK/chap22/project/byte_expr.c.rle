#include <stdio.h>
int main(void) {
	int ch;
	int n = 0;
	while (ch = getchar() != EOF) {
	if (n+ % 20 = 0)
	printf("\n");
	printf("%.2x ", ch);
	}
	printf("\n");
	return 0;
}