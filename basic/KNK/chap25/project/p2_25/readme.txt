Write a program that obtains the name of a locale from the command line and
then displays the values stored in the corresponding lcov structure.
For readability, the characters in grouping and mon_grouping should be displayed
as decimal numbers.
